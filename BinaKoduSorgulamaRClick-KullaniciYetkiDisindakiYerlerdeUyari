diff --git a/src/app/dialog-info/dialog-info.component.html b/src/app/dialog-info/dialog-info.component.html
index 54c39e862213354d23750d4adeb28d215db729e3..f6678cccc293a8c0374cc0792a12db869ad6d5ab 100644
--- a/src/app/dialog-info/dialog-info.component.html
+++ b/src/app/dialog-info/dialog-info.component.html
@@ -1,23 +1,24 @@
 <div class="modal-content">
-    <p>{{dialogConfig.data.message}}</p>
-    <div *ngIf="this.maksBinaKoduSorgulaState">
-        <table>
-            <thead>
-                <tr>
-                    <th>Dış Kapı No</th>
-                    <th>Kimlik No</th>
-                    <th>Bina Kodu</th>
-                </tr>
-            </thead>
-            <tbody>
-                <tr *ngFor="let item of binaList">
-                    <td>{{item.kapiNo}}</td>
-                    <td>{{item.kimlikNo}}</td>
-                    <td>{{item.binaKodu}}</td>
-                </tr>
-            </tbody>
-        </table>
-    </div>
-
-    <div *ngIf="notificationMessage !== ''"> {{notificationMessage}} </div>
+  <p>{{dialogConfig.data.message}}</p>
+  <div class= "table-container" *ngIf="this.maksBinaKoduSorgulaState">
+    <table>
+      <thead>
+        <tr>
+          <th>Dış Kapı No</th>
+          <th>Kimlik No</th>
+          <th>Bina Kodu</th>
+        </tr>
+      </thead>
+      <tbody>
+        <tr *ngFor="let item of binaList">
+          <td>{{item.kapiNo}}</td>
+          <td>{{item.kimlikNo}}</td>
+          <td>{{item.binaKodu}}</td>
+        </tr>
+      </tbody>
+    </table>
+  </div>
+  <div *ngIf="notificationMessage !== ''">
+    <p>{{notificationMessage}}</p>
+  </div>
 </div>
diff --git a/src/app/dialog-info/dialog-info.component.ts b/src/app/dialog-info/dialog-info.component.ts
index ff2271361853d02fd4d733da333029c23b6cc333..acbeb123a7ff7efaa9a86414fab34ccb2268c80f 100644
--- a/src/app/dialog-info/dialog-info.component.ts
+++ b/src/app/dialog-info/dialog-info.component.ts
@@ -31,18 +31,17 @@ export class DialogInfoComponent implements OnInit {
   ilceProperties;
   pointModule: esri.PointConstructor;
   isPermitted;
-  constructor(public dialogConfig: DialogConfig, public dialog: DialogRef, private dialogService: DialogService,
-    private webDataService: WebDataService, private auth: AuthService) {
 
-  }
+  constructor(public dialogConfig: DialogConfig, public dialog: DialogRef, private dialogService: DialogService,
+    private webDataService: WebDataService, private auth: AuthService) {}
 
   ngOnInit() {
+    this.notificationMessage = '';
     this.getModules().then(moduleRes => {
       this.pointModule = moduleRes;
       this.userPerm = this.auth.userPerm;
       const data = this.dialogConfig.data;
       this.mapView = data.mapView;
-      this.maksBinaKoduSorgulaState = data['maksBinaKoduSorgulaState'];
       this.lat = data['coordinateData']['lat'];
       this.lon = data['coordinateData']['lon'];
       this.ilceLayer = data['ilceLayer'];
@@ -55,7 +54,7 @@ export class DialogInfoComponent implements OnInit {
         if (this.isPermitted === 1) {
           this.getAllNumaratajFromBina(this.lat, this.lon);
         } else {
-          // TODO: İzinli Değilsiniz Uyarısı Çıksın.
+          this.notificationMessage = 'Sorgulama Yapılmak İstenen Bölge Yetki Dahilinde Değildir.';
           console.log('YOU ARE NOT PERMITTED IN');
         }
       });
@@ -104,13 +103,14 @@ export class DialogInfoComponent implements OnInit {
 
     if (foundCity === undefined) { // izinli il listesinde bulamazsa, izinli ilçe listesine bakılacak.
       let foundDistrict;
-      this.permittedDistricts.forEach(district => {
-        const foundCode = district.find(code => Number(code) === kodilce);
+
+      for (let i = 0; i < this.permittedDistricts.length; i++) {
+        const foundCode = this.permittedDistricts[i].find(code => Number(code) === kodilce);
         if (foundCode !== undefined) {
           foundDistrict = foundCode;
+          break;
         }
-        // TODO: FOREACH ERKEN DURDURMA YAPILACAK
-      });
+      }
 
       if (foundDistrict === undefined) {
         return -1;
@@ -137,15 +137,13 @@ export class DialogInfoComponent implements OnInit {
 
   getAllNumaratajFromBina(lat: string, lon: string) {
     this.webDataService.getAllNumaratajFromBina(lat, lon).subscribe(res => {
-      if (res !== null || res !== undefined) {
+      if (res !== null && res !== undefined) {
         this.binaList = res;
+        this.maksBinaKoduSorgulaState = this.dialogConfig.data['maksBinaKoduSorgulaState'];
       } else {
         this.maksBinaKoduSorgulaState = false;
         this.notificationMessage = 'Maks:Yapi ve Maks:DigerYapida Veri Bulunamadı.';
-        // TODO: res NUll dönüyorsa maks:yapi ve maks:digeryapida veri bulamamış demek info dialog ile bulunamadı uyarısı versin
       }
     });
   }
-  // TODO: SORGULAMA YAPILIRKEN PROGRESSINFO COMPONENTİ EKLENECEK
-  // TODO: TABLO HTML CSS DÜZENLENECEK
 }
